import { ACTIVE_PATH } from "./active-path";
import { AUTH_PATH } from "./auth.path";
import { BASE_PATH } from "./base.path";
import { ERROR_PATH } from "./error.path";
import { MAIN_PATH } from "./main.path";

export const PATH = {
    AUTH_PATH,
    ERROR_PATH,
    BASE_PATH,
    MAIN_PATH,
    ACTIVE_PATH,
};
