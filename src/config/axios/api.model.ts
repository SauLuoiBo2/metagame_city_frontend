export interface ResponseProps<T> {    success: string;    code: number;    locale: string;    message: string;    data: T;}export interface MetaProps {    current_page: number;    from: number;    last_page: number;    per_page: number;    to: number;    total: number;}export interface DataListResponseProps<T> {    data: T[];    link: any;    meta: MetaProps;}